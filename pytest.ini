[pytest]
testpaths = tests src_lab/tests
markers =
    live: marks tests that require a running Azure Function (deselect with '-m "not live"')
    mock: marks tests that can run fully mocked (no Azure needed)
asyncio_mode = auto
