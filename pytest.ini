[pytest]
testpaths = tests
markers =
    live: marks tests that call deployed Azure resources (deselect with '-m "not live"')
    mock: marks tests that can run fully mocked (no Azure needed)
asyncio_mode = auto
